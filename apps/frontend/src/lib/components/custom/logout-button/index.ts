import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from "./logout-button.svelte";

export {
	Root,
	type ButtonProps as Props,
	//
	Root as LogoutButton,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};
